spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/aadi
    username: aadi
    password: aadi
  jpa:
    hibernate:
      ddl-auto: update # create and destroy schema at the end of session
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet
